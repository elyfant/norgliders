# SOPs & Runbooks

One file per system. Each should answer: where does it run, how do you restart it, who do you call if it breaks, and what does "healthy" look like.

Existing SOPs already exist but are scattered across various formats. First pass here is collating and standardizing them into this directory under version control — not writing new ones from scratch. See system map item 8.
